spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reports_db
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  sql:
    init:
      mode: always